# Backend

Simple Express API used by the NATPAC frontend.

## Run

```bash
npm install
npm run dev
```

The server starts on `http://localhost:4000`.

## Endpoints

- `GET /api/health`
- `POST /api/auth/login`
- `GET /api/admin/users`
- `GET /api/admin/heatmap`
